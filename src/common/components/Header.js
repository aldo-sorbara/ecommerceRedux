import React from 'react';
import styled from 'styled-components';
// import PropTypes from 'prop-types';

const Header = ({}) => <StyledHeader>TEST</StyledHeader>;

const StyledHeader = styled.header`
  background: red;
`;

Header.propTypes = {};

export default Header;
